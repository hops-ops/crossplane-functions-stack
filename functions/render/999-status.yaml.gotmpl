# code: language=yaml
{{- $xr := getCompositeResource . }}
{{- $s := $state.status }}
---
apiVersion: {{ $xr.apiVersion }}
kind: {{ $xr.kind }}
status:
  ready: {{ $s.ready }}
  functions:
    {{- range $key, $f := $s.functions }}
    {{ $key }}:
      ready: {{ $f.ready }}
    {{- end }}
